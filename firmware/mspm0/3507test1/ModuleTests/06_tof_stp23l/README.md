# 06 STP-23L 测距测试

目标：完成 STP-23L 供电、UART 接收、帧校验、超时和距离稳定性测试。

## 接线

- 先查实物标签/资料确认供电和 UART 逻辑电平；
- 传感器 TX 接 MSPM0 RX，传感器 RX 接 MSPM0 TX，共地；
- 5V UART 输出不得直接接入 MSPM0；
- 波特率、帧头、长度和校验方式写入接口冻结表。

## 验收

- 已知距离点重复测量并记录误差；
- 处理无效帧、超量程和串口超时；
- 拔掉传感器后主循环不阻塞，并产生错误码；
- 连续运行 30 分钟无接收卡死。
