# 07 MaixCAM Pro 视觉串口测试

目标：视觉模块独立处理图像，仅通过 UART 向 MSPM0 输出坐标、类别和状态。

## 接线

- MaixCAM 使用独立、稳定且经验证的 5V 支路；
- UART 必须确认是 3.3V 逻辑，TX/RX 交叉并共地；
- 协议必须包含帧头、长度、消息类型、数据、校验和超时；
- 视觉失联不得阻塞电机控制与安全逻辑。

## 验收

- 固定目标下连续接收 100 帧并统计有效率；
- 模拟错帧、丢帧和拔线；
- MSPM0 在超时后清除旧目标并进入安全状态；
- K230/MaixCAM 不直接控制电机。
